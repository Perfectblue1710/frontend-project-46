name: hexlet-check

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Run hexlet tests
        uses: hexlet-components/hexlet-check-action@v1
